/**:
  ros__parameters:
    pcTopic: /pc_interpoled
    rawPcTopic: /velodyne_points
    imgTopic: /camera/camera/color/image_raw
    image_transport_mode: auto
    sync_queue_size: 10

    overlay_percentile_low: 0.05
    overlay_percentile_high: 0.95
    overlay_range_smoothing_alpha: 0.2

    output_cloud_topic: /points2
    # Interpolated-cloud camera overlay (kept on the existing topic).
    output_image_topic: /pcOnImage_image
    # Original VLP-16 cloud camera overlay.
    output_raw_image_topic: /pcOnImage_raw_image
